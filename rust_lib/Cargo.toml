[package]
name = "rust_lib"
version = "0.1.0"
edition = "2021"

[lib]
name = "rust_lib"
crate-type = ["cdylib"] # <--- 关键！编译成动态库 (.so / .pyd)

[dependencies]
# extension-module 特性表示我们要构建 Python 扩展
pyo3 = { version = "0.20", features = ["extension-module"] }